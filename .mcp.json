{
  "mcpServers": {
    "directus": {
      "command": "npx",
      "args": ["-y", "@staminna/directus-mcp-server"],
      "env": {
        "DIRECTUS_URL": "${DIRECTUS_URL:-https://modest-presence-production-e81d.up.railway.app}",
        "DIRECTUS_TOKEN": "${DIRECTUS_TOKEN}"
      }
    }
  }
}
