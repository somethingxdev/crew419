# Directus (content source). Copy to .env — never commit real values.
DIRECTUS_URL=https://modest-presence-production-e81d.up.railway.app
# Static token of a Directus user with read access to all content collections.
DIRECTUS_TOKEN=
