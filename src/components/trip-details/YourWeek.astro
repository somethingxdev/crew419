---
import type { CollectionEntry } from 'astro:content';
import CmsImage from '@/components/base/CmsImage.astro';
import SectionHeading from '@/components/shared/SectionHeading.astro';

interface Props {
  trip: CollectionEntry<'trips'>;
}

// The photo next to the itinerary is the trip's card image — no separate upload.
const { itinerary: days, card_image: media, title } = Astro.props.trip.data;
const mediaClass = 'h-auto w-full rounded-lg object-cover lg:h-134.5 lg:flex-1';
---

<section class="py-15" aria-labelledby="your-week-heading">
  <div class="container">
    <SectionHeading id="your-week-heading" eyebrow="Your week" heading="What Your Mission Week Could Look Like" headingClass="max-w-164" />

    <div class="mt-12.5 flex flex-col gap-12.5 lg:flex-row lg:items-start lg:gap-20">
      <ul class="flex w-full flex-col max-w-[485px]">
        {
          days.map((day) => (
            <li class="flex flex-col gap-4 py-6 md:py-12.5 first:pt-0 border-b border-muted-foreground">
              <h3 class="text-2xl font-bold">{day.title}</h3>
              <p class="text-lg leading-tight">{day.text}</p>
            </li>
          ))
        }
      </ul>

      <CmsImage image={media} alt={title} widths={[800, 1600]} class={mediaClass} />
    </div>
  </div>
</section>
