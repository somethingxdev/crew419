---
import { getSingleton } from '@/lib/content';

const {
  churches_intro_eyebrow: eyebrow,
  churches_intro_heading: heading,
  churches_intro_text: text,
  churches_challenge_cards: challenges,
} = await getSingleton('website_content');
---

<section class="relative z-20 py-30" aria-labelledby="why-partner-heading">
  <div class="container flex flex-col items-center gap-12.5">
    <div class="flex max-w-195 flex-col items-center gap-7.5 text-center">
      <div class="flex flex-col items-center gap-4">
        <p class="text-muted-foreground leading-none font-bold uppercase">{eyebrow}</p>
        <h2 id="why-partner-heading" class="text-3xl  font-bold md:text-5xl">{heading}</h2>
      </div>
      <p class="max-w-124 leading-none">{text}</p>
    </div>

    <div class="grid w-full gap-5 md:grid-cols-2 xl:grid-cols-4">
      {
        challenges.map((item) => (
          <article class="bg-cream flex min-h-49 flex-col justify-between gap-5 rounded-lg p-7.5">
            <h3 class="text-muted-foreground text-2xl  font-bold">{item.title}</h3>
            <p class="text-lg leading-none font-medium">{item.text}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>
