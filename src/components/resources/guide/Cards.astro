---
import type { CollectionEntry } from 'astro:content';
import SectionHeading from '@/components/shared/SectionHeading.astro';

interface Props {
  resource: CollectionEntry<'resources'>;
}

// Only guides (discipleship / fundraising) have a cards section — travel guides leave these fields empty.
const { cards_eyebrow: eyebrow, cards_heading: heading, cards } = Astro.props.resource.data;
---

{
  eyebrow && heading && cards && (
    <section class="pt-40 pb-15" aria-labelledby="guide-cards-heading">
      <div class="container">
        <SectionHeading id="guide-cards-heading" {eyebrow} {heading} headingClass="leading-none" class="mx-auto max-w-169" />

        <ul class="mt-12.5 grid gap-5 sm:grid-cols-2 xl:grid-cols-4">
          {cards.map((card) => (
            <li class="bg-cream flex min-h-44.5 flex-col justify-between gap-7.5 rounded-lg p-7.5">
              <h3 class="text-2xl font-bold text-muted-foreground">{card.title}</h3>
              <p class="text-lg leading-none font-medium">{card.text}</p>
            </li>
          ))}
        </ul>
      </div>
    </section>
  )
}
